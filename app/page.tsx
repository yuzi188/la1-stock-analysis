"use client";

// TEMPORARY RESTORE MARKER - full content follows in next commit if truncated
export default function Home() {
  return (
    <main style={{ padding: 24, color: "#e8eef6", background: "#03080d", minHeight: "100vh" }}>
      <h1>LA1 台股分析室</h1>
      <p>page.tsx 正在還原中，請稍候重新部署。</p>
      <p>若持續看到此頁，請用 la1-trading-deploy.zip 覆蓋 app/page.tsx 後 push。</p>
    </main>
  );
}
